# fnn-from-scratch-for-mnist
A simple feedforward neural network implementation. The model is built from scratch in Python with numPy only, with explicit forward pass, loss function, and backpropagation implementation.
